version: "3.9"

services:

  sandoghchi-api:

    build: .

    container_name: sandoghchi-api

    ports:
      - "8080:8080"

    env_file:
      - .env

    volumes:
      - ./data:/app/data


    restart: always


